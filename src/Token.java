/**
 * this class holds the token ID
 */
public class Token {
    int tokenID;

    /**
     * getter for token
     *
     * @param tokenID
     */
    public Token(int tokenID) {
        this.tokenID = tokenID;
    }
}
